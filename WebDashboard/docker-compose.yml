version: '3.8'

services:
  ai-widget-dashboard:
    build: .
    container_name: ai-widget-dashboard
    ports:
      - "9000:9000"
    volumes:
      - ai-widget-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=9000

volumes:
  ai-widget-data:
    external: true
