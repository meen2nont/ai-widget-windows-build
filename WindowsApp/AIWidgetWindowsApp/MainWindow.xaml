<Window x:Class="AIWidgetWindowsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIWidgetWindowsApp"
        xmlns:viewmodels="clr-namespace:AIWidgetWindowsApp.ViewModels"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="AI Widget" Height="450" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Window.DataContext>
        <viewmodels:DeepSeekViewModel />
    </Window.DataContext>

    <!-- System Tray Icon -->
    <tb:TaskbarIcon
        x:Name="MyNotifyIcon"
        IconSource="/deepseek_favicon.ico"
        ToolTipText="AI Widget">
        
        <!-- Tray Context Menu -->
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Show" Click="MenuItemShow_Click"/>
                <MenuItem Header="Exit" Click="MenuItemExit_Click"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>

    <!-- Main UI Popup content -->
    <Border Background="#1E1E1E" CornerRadius="12" Padding="20">
        <StackPanel>
            <TextBlock Text="AI Widget - DeepSeek" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Balance: " Foreground="LightGray"/>
                <TextBlock Text="{Binding Balance}" Foreground="White" FontWeight="Bold"/>
                <TextBlock Text=" " Foreground="White" />
                <TextBlock Text="{Binding Currency}" Foreground="White"/>
            </StackPanel>

            <TextBox Text="{Binding PromptText, UpdateSourceTrigger=PropertyChanged}" 
                     Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                     Background="#2D2D2D" Foreground="White" BorderThickness="0"
                     Margin="0,0,0,10" Padding="5"/>
                     
            <Button Content="Send" Command="{Binding SendCommand}" 
                    Background="#007ACC" Foreground="White" Padding="10,5" BorderThickness="0"
                    HorizontalAlignment="Right" Margin="0,0,0,20"/>
                    
            <TextBlock Text="Response:" Foreground="LightGray" Margin="0,0,0,5"/>
            <ScrollViewer Height="120">
                <TextBlock Text="{Binding AiResponse}" Foreground="White" TextWrapping="Wrap"/>
            </ScrollViewer>
            
            <Button Content="Copy" Command="{Binding CopyToClipboardCommand}" 
                    Background="#333333" Foreground="White" Padding="10,5" BorderThickness="0"
                    HorizontalAlignment="Right" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</Window>
