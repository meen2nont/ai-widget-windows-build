<UserControl x:Class="AIWidgetWindowsApp.Views.OverviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <!-- DeepSeek Card -->
        <Border Background="#1A007ACC" CornerRadius="10" BorderBrush="#33007ACC" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="✨" Foreground="Cyan" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="DeepSeek" Foreground="White" FontWeight="Bold" FontSize="12"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding DeepSeekVM.Balance, StringFormat=${0}}" Foreground="White" FontWeight="Bold" FontSize="13" TextAlignment="Right"/>
                    <TextBlock Text="{Binding DeepSeekVM.SpentToday, StringFormat=Spent Today: ${0}}" Foreground="Gray" FontSize="10" TextAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Ollama Cloud Card -->
        <Border Background="#1AFF8C00" CornerRadius="10" BorderBrush="#33FF8C00" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="☁️" Foreground="Orange" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="Ollama Cloud" Foreground="White" FontWeight="Bold" FontSize="12"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding OllamaVM.SessionUsagePercent, StringFormat={}{0:F1}% Session}" Foreground="White" FontWeight="Bold" FontSize="13" TextAlignment="Right"/>
                    <TextBlock Text="{Binding OllamaVM.WeeklyUsagePercent, StringFormat=Weekly: {0:F1}% used}" Foreground="Gray" FontSize="10" TextAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Ollama Pay Card -->
        <Border Background="#1A800080" CornerRadius="10" BorderBrush="#33800080" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="💳" Foreground="Purple" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="Ollama Pay" Foreground="White" FontWeight="Bold" FontSize="12"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding OllamaPayVM.TodayTokens, StringFormat={}{0:N0} tokens}" Foreground="White" FontWeight="Bold" FontSize="13" TextAlignment="Right"/>
                    <TextBlock Text="{Binding OllamaPayVM.MonthTokens, StringFormat=Month: {0:N0}}" Foreground="Gray" FontSize="10" TextAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
